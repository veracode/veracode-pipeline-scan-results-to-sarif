export interface Options {
    inputFilename: string,
    outputFilename: string,
    ruleLevel: string,
    pathReplacers: string,
    repo_owner: string,
    repo_name: string,
    githubToken: string
}